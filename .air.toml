[build]
  cmd = "go build -o ./tmp/main.exe ./cmd/server"
  bin = "./tmp/main.exe"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "tests"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
